# ── RPC (defaults are public endpoints; a paid key removes the 10k-block log cap) ──
RPC_ETHEREUM=https://eth.drpc.org
RPC_BASE=https://base.drpc.org

# ── The Graph ──
GRAPH_API_KEY=
SUBGRAPH_URL=

# ── Hedera: x402 metering + HCS attestation archive ──
HEDERA_NETWORK=testnet
HEDERA_ACCOUNT_ID=
HEDERA_PRIVATE_KEY=
HCS_TOPIC_ID=
X402_NETWORK=hedera-testnet
X402_ASSET=HBAR
X402_PAY_TO=
X402_FACILITATOR=https://facilitator.blocky402.com
# Set to 1 for local development only. Never in a deployed demo.
X402_DEV_BYPASS=0
